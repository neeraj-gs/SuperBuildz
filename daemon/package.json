{
  "name": "@superbuilds/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.3",
    "@superbuilds/protocol": "*",
    "fastify": "^5.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0"
  },
  "//playwright-core": "Optional and loaded lazily: reference capture and thumbnails want it, nothing else does. A missing browser is reported on the requirements screen with a button.",
  "optionalDependencies": {
    "playwright-core": "^1.50.0"
  }
}
